# ระบบสลับกุญแจ ElevenLabs อัตโนมัติ (API Key Rotation)

เพิ่มระบบหมุนเวียนกุญแจ API แบบอัตโนมัติ เพื่อรองรับการใช้งาน ElevenLabs แบบต่อเนื่อง (เมื่อโควต้าของกุญแจดอกแรกเต็ม โปรแกรมจะดึงกุญแจดอกที่ 2 มาใช้ต่อทันทีโดยไม่สะดุด)

## การเปลี่ยนแปลงที่จะเกิดขึ้น

### 1. ไฟล์ .env (ผู้ใช้ต้องแก้ไขเอง)
- เพิ่มตัวแปรใหม่ชื่อ ELEVENLABS_API_KEYS (เติม S)
- วิธีใส่คือ นำ API Key ทั้ง 5 ตัวมาวางต่อกันคั่นด้วยเครื่องหมายจุลภาค (,) เช่น:
  ELEVENLABS_API_KEYS=sk_key1,sk_key2,sk_key3,sk_key4,sk_key5

### 2. ไฟล์ src/elevenlabs_gen.py
#### [MODIFY] elevenlabs_gen.py
- ปรับปรุงฟังก์ชันให้ดึงค่ากุญแจทั้งหมดมาสร้างเป็น "พวงกุญแจ" (List of Keys)
- เพิ่มระบบ Loop (วนลูป) นำกุญแจดอกที่ 1 ไปไขดูก่อน
- ถ้าเซิร์ฟเวอร์แจ้งกลับมาว่า Error 401 (โควต้าเต็ม หรือกุญแจพัง) โปรแกรมจะแสดงข้อความว่า ⚠️ กุญแจดอกที่ 1 โควต้าเต็ม! กำลังสลับไปใช้ดอกที่ 2... แล้วทำการยิงคำสั่งพากย์เสียงใหม่อัตโนมัติ
- วนไปเรื่อยๆ จนกว่าจะพากย์สำเร็จ หรือจนกว่ากุญแจจะหมดพวง

## ข้อควรระวัง (User Review Required)
> [!IMPORTANT]
> ระบบนี้ต้องการให้คุณสิทธิ์เข้าไปแก้ไขไฟล์ .env ด้วยตัวเอง โดยพิมพ์ชื่อตัวแปรให้ถูกต้องคือ ELEVENLABS_API_KEYS (เติม S) และห้ามมีช่องว่างระหว่างลูกน้ำ (,)
